name: Compilar CÓDICE para cPanel

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Descargar el código
        uses: actions/checkout@v4

      - name: Preparar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Instalar dependencias
        run: npm install

      - name: Compilar (para la subcarpeta /codice/, SIN comprimir — para poder leer errores)
        run: npm run build -- --base=/codice/ --minify=false --sourcemap

      - name: Empaquetar el resultado
        run: cd dist && zip -r ../codice-compilado.zip .

      - name: Dejarlo disponible para descargar
        uses: actions/upload-artifact@v4
        with:
          name: codice-compilado
          path: codice-compilado.zip
